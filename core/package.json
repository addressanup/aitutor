{
  "name": "@aitutor/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env src/main.ts",
    "demo": "node tools/demo.ts",
    "fakeshell": "node tools/fake-shell.ts",
    "journal-dump": "node --env-file-if-exists=.env tools/journal-dump.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aitutor/protocol": "workspace:*",
    "pino": "^9.5.0",
    "yaml": "^2.6.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "@types/ws": "^8.5.0",
    "pino-pretty": "^13.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0",
    "ws": "^8.18.0"
  }
}

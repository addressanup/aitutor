# Node
node_modules/
dist/
coverage/
*.tsbuildinfo

# env & local data
.env
.env.*
!.env.example
.data/
*.db
*.db-wal
*.db-shm

# Swift / Xcode
.build/
.swiftpm/
DerivedData/
xcuserdata/
*.xcodeproj
shell/build/

# macOS
.DS_Store

# Agent worktrees — 740MB of nested checkouts, each with its own biome.json,
# which biome reads as a competing root config and refuses to lint past.
.claude/worktrees/
